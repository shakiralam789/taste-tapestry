/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Use `npm run build` (with --webpack) if Turbopack prerender causes QueryClient errors
};

export default nextConfig;
